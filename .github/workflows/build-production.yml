name: Publish production images

# A v* tag releases BOTH apps in lockstep: backend and frontend production images
# are built together, so a deployed pair always comes from the same commit.

on:
  push:
    tags:
      - 'v*'

jobs:
  date_sha_tag:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.image_tag.outputs.tag }}
    steps:
      - id: image_tag
        run: echo "tag=$(date +%s)-${{ github.sha }}" >> "$GITHUB_OUTPUT"

  publish_backend:
    needs: date_sha_tag
    # Same grant as the two main workflows, for the same reason — see
    # publish-docker-image.yml. No `v*` tag has been pushed yet, so this
    # workflow has never run; it would have failed at startup exactly as its
    # siblings did.
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/publish-docker-image.yml
    with:
      context: apps/directus
      tags: |
        ghcr.io/${{ github.repository }}-backend:production-${{ needs.date_sha_tag.outputs.tag }}
        ghcr.io/${{ github.repository }}-backend:production
    secrets: inherit

  publish_front:
    needs: date_sha_tag
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/publish-docker-image.yml
    with:
      context: apps/front
      tags: |
        ghcr.io/${{ github.repository }}-front:production-${{ needs.date_sha_tag.outputs.tag }}
        ghcr.io/${{ github.repository }}-front:production
    secrets: inherit
